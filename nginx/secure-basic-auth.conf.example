# nginx/default.conf 已启用以下配置：
# auth_basic "Investment Overview";
# auth_basic_user_file /etc/nginx/.htpasswd;
#
# 仓库不提供默认密码文件。请在仓库外生成独立文件，并通过
# BASIC_AUTH_FILE=/absolute/path/to/secure.htpasswd 指定挂载来源。
