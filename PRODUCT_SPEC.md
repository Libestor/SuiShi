# 投资总览产品规格

> 状态：MVP 设计基线（已实现首版）
> 最近更新：2026-08-10
> 本文档记录当前已经确认的产品方向；标有“待确认”的内容尚未定案。

## 1. 产品定位

这是一个面向个人使用的投资资产记录、配置与成长展示平台。

平台的核心目的：

- 直观呈现当前资产、篮子比例和目标进度。
- 记录资金从外部流入、分配到篮子、购买产品以及价值变化的完整过程。
- 通过资产树、增长曲线、里程碑和成就展示，强化资产逐渐增长带来的安全感与成就感。
- 使用固定规则帮助用户分配新增资金，但不替用户选择投资产品。
- 通过可编程数据源更新价格、汇率等外部数据。

当前明确不做：

- 不推荐股票、基金或其他具体产品。
- 不提供选股、选基能力。
- 不连接投资平台执行买入或卖出。
- 不追溯平台启用之前的历史交易。
- 第一版不设计任意数量和任意逻辑的顶层篮子。

## 2. 使用场景与终端

### 2.1 手机端

手机端以查看和激励为主：

- 查看资产树和总资产。
- 查看目标及里程碑进度。
- 查看篮子比例、增长曲线和数据新鲜度。
- 查看通知和待确认事项。
- 展示成就与解锁动画。

### 2.2 电脑端

电脑端承担主要管理工作：

- 管理资产、流水、篮子和定投计划。
- 手动更新无法自动获取的数据。
- 统一编辑资产的篮子、平台、代码、份额、单价、汇率、数据源属性与备注。
- 配置数据源、字段映射和更新频率。
- 编辑、测试和启停 Python 脚本。
- 查看脚本执行日志和 Git 版本，执行版本回退。
- 使用更完整的专业分析视图。
- 在设置页管理资金流入模式、目标比例、应急储备计算和外部推送连接。

实际投资操作仍在券商、银行、基金平台等外部平台完成。

## 3. 固定篮子模型

第一版固定三个顶层篮子：

1. 应急储备金
2. 成长性投资
3. 高风险投资

产品属于成长篮子还是高风险篮子，完全由用户指定，平台不做风险分类判断。

平台是资产的属性，不作为资产树的固定层级。平台维度可以通过单独的统计视图查看。

### 3.1 应急储备金

应急储备金拥有最高资金补充优先级。

- 用户可直接填写目标金额。
- 平台提供必要支出计算器，输入房租、伙食费、水电费等支出项目。
- 默认建议保留 6 个月必要支出。
- 建议金额向上取整到千元。
- 用户可以覆盖建议金额，并添加计算说明或备注。
- 当储备金低于目标时，后续新增资金优先全部补入该篮子。
- 补满后，剩余资金才进入成长性投资和高风险投资篮子。
- 应急储备金原则上使用保守产品；从中取出资金后会重新触发优先补充。

### 3.2 成长与高风险篮子

应急储备金补满后，剩余资金进入成长与高风险篮子。支持两种分配模式：

- **动态平衡**：根据两个篮子的最新价值与目标比例，将新增资金优先分给比例偏低的一方，尽可能恢复目标比例。平台不自动卖出现有资产。
- **固定分配**：忽略当前篮子偏差，只按预设比例拆分新增资金。

两种模式都需要保留，并允许用户选择。

篮子总资产包含：

- 篮子中已购买产品的最新已知价值。
- 已经归属该篮子、但仍等待购买的现金。

等待购买的现金价值保持稳定，但**不参与成长与高风险之间的风险比例计算**。风险比例和动态分配只比较两个篮子内已经购买、且平台拥有最新已知估值的产品；当任一产品价格变化或用户手动更新后，再使用新的已投资价值重新计算。等待购买现金仍计入篮子金额和总资产，并在树状结构中单独呈现。

## 4. 资产模型

### 4.1 固定核心字段

第一版围绕投资场景使用固定的核心资产模型，不建设通用低代码数据平台。

| 字段 | 规则 |
| --- | --- |
| 资产 ID | 平台生成，脚本不可修改 |
| 资产名称 | 用户填写，脚本不可修改 |
| 所属篮子 | 用户选择，脚本不可修改 |
| 所属平台 | 用户填写，脚本不可修改 |
| 持有份额 | 创建资产时必填 |
| 当前单价 | 创建资产时必填，可手动更新或由数据源更新 |
| 原始币种 | 创建资产时选择 |
| 当前原币价值 | 平台计算：持有份额 × 当前单价 |
| 当前人民币价值 | 平台计算：当前原币价值 × 最新汇率 |
| 最后更新时间 | 平台在数据成功更新时写入 |
| 数据源属性 | 用户配置，例如股票代码、基金代码或交易所代码 |

常规产品使用 `持有份额 × 当前单价` 计算价值。

对于不方便拆分份额和单价、只方便查看总余额的产品，可以约定：

```text
持有份额 = 1
当前单价 = 当前总余额
```

### 4.2 多币种

- 平台基础币种为人民币。
- 资产保留原始币种和原币单价。
- 汇率可由数据源自动更新，也可由用户手动维护。
- 单价和汇率分别显示数据来源及最后更新时间。

### 4.3 数据新鲜度

- 数据不会因长时间未更新而过期、删除或停止参与计算。
- 平台始终使用最后一次已知数据。
- 界面显示数据来源、最后更新时间以及距今多久未更新。
- 使用较旧数据生成比例或分配结果时，明确提示可能存在偏差。

## 5. 流水账与期初数据

平台采用流水账模式，区分资金投入、内部移动和估值变化，避免重复计算资产增长。

基本原则：

- 外部新增资金会增加总资产。
- 从现金购买产品只是内部转移，不会再次增加总资产。
- 产品价格变化形成估值变化，不作为外部投入。
- 无法自动监控的变化由用户手动补录。

首次启用平台时创建“期初快照”：

- 记录启用日期及当时的持仓和价值。
- 期初资产作为曲线起点，不算新增投入，也不算平台启用后的投资收益。
- 平台不要求补录过去的历史交易。
- 此后的资金流水和估值变化进入完整统计。

具体流水类型、手续费、分红、利息和手动修正方式仍待确认。

## 6. 定投与新增资金

定投计划与真实资产分离。

```text
定投计划到期
    ↓
生成待确认任务和分配建议
    ↓ 用户确认实际到账
创建真实资金流水
    ↓
资金进入相应篮子的等待购买现金
    ↓ 实际购买或数据同步
形成具体产品持仓
```

规则：

- 定投到期不会直接增加真实资产。
- 用户确认资金实际到账后，才创建真实流水。
- 新增资金先补足应急储备金。
- 剩余资金按动态平衡或固定分配模式进入后两个篮子。
- 暂时无法购买的部分保留为对应篮子的等待购买现金。
- 数据源更新或用户手动更新后，平台使用最新已知价值重新计算比例。

待确认：分配建议是否允许在确认前手动修改，以及如何记录计划值与实际值的差异。

## 7. 数据展示与专业分析

平台同时提供成长意象和专业分析能力，两者使用同一套底层数据。

### 7.1 资产生命树

- 使用计算机树形图，而不是具象植物插画。
- 最上方是总资产根节点，显示总金额和 100% 占比。
- 第二层固定为三个篮子节点，分别显示金额、占总资产比例和用途说明。
- 每个篮子可以独立展开或收起；展开后显示具体产品和待购买现金。
- 产品节点显示金额、篮内占比、平台和最后更新时间。
- 父子节点通过清晰的连接线表达归属关系，任意一层都能直观看出金额如何组成。
- 里程碑达成时可触发开花、结果、增加年轮或纪念物等动画。

### 7.2 专业仪表盘

- 总资产及三个篮子的当前金额。
- 成长与高风险篮子的目标比例、当前比例及偏差。
- 各产品价值、占比、平台、币种和数据更新时间。
- 等待购买现金和待确认任务。
- 数据源运行状态及数据新鲜度。
- 专业比例图、资产结构图和历史曲线。

### 7.3 资产增长曲线

主曲线包含：

- **总资产线**：所有篮子换算成人民币后的当前价值。
- **累计本金线**：期初资产 + 后续净投入 - 取出。
- **投资盈亏区域**：总资产与累计本金之间的差额。
- 支持切换查看各篮子的独立曲线。
- 曲线以自然月聚合，每月使用该月最后一份快照；当月展示最新快照，跨月后固定，不随后续价格或分类变化回算。
- 支持查看最近 6 个月、1 年、3 年和全部历史。
- 月度详情显示当前金额、累计本金、累计盈亏、盈亏率、当月净投入和相对上月金额变化。
- 每个篮子独立展示当前金额、本金、盈亏、当月净投入及报价新鲜度。
- 篮子本金只由该篮子的期初、外部存入和外部取出改变；篮子内买卖、分红、利息和市场涨跌不改变本金。

### 7.4 历史估值频率

- 默认每小时获取并保存一次资产估值。
- 用户可以调整数据源和资产的更新频率。
- 用户可以随时手动触发更新。
- 每次成功更新形成历史估值记录，当前值取最后一次成功结果。
- 数据源执行失败不会覆盖最后一次成功数据，失败信息进入执行日志。

## 8. 总资产目标、里程碑与成就

### 8.1 总资产目标

- 目标金额使用人民币。
- 当前进度包含三个篮子的全部资产。
- 展示完成比例、已完成金额和剩余金额。
- 可填写目标日期和备注。
- 可根据近期净投入速度估算达成时间，估算不将未来投资收益视为确定结果。

### 8.2 里程碑和成就

- 用户自行创建里程碑名称、目标金额、说明、图标或封面。
- 首页实时展示当前进度。
- 达成后永久保存，不因市场回撤或资产取出而撤销。
- 保存解锁时间、当时的总资产、篮子比例、资产快照和增长曲线。
- 支持解锁动画和永久成就陈列。
- 成就与树的成长表现可以联动。

### 8.3 奖励展示

奖励由用户自行定义，平台仅提供呈现框：

- 奖励标题。
- 奖励说明。
- 图片、图标或封面。
- 解锁后的展示状态。

平台不解释奖励、不自动兑换，也不自动产生资金流水。

## 9. Python 数据源

### 9.1 基本形式

- 数据源使用 Python 3 编写。
- 网页内提供代码编辑器。
- 自动化页以脚本列表呈现，每条数据源对应一个可单独打开、修改、调度和回退的脚本。
- 数据源本质上是接收字典并返回字典的函数。
- 支持一次输入一项或多项资产，并一次返回一项或多项结果。
- 每个脚本保存绑定资产集合；例如一个“基金净值”脚本可持续绑定所有新增基金资产。
- 资产属性通过输入映射传给函数。
- 函数返回值通过输出映射更新允许由脚本维护的资产数据。
- 数据源可配置手动、每小时、每天、每周等执行频率。
- 每次成功更新由平台记录数据来源和更新时间。

数据源首版只允许更新资产单价、汇率和 `source_attributes` 下的扩展属性；名称、资产 ID、所属篮子、平台与持有份额不可由脚本修改。

### 9.2 编辑、版本和运行

- 脚本可在网页中编辑、测试、启停和查看日志。
- 使用 Git 保存版本和差异，主要用于回退。
- Python 脚本通过独立 Docker 运行环境执行。
- 平台本身使用数据库保存资产、流水、估值、脚本配置和运行记录。
- 主应用向脚本传递输入，脚本返回结果，由主应用完成映射和数据库写入。
- Python 项目和数据源运行环境使用 `uv` 管理依赖。
- 平台提供添加 Python 包的能力。
- 添加或变更依赖需要留下操作与构建日志；具体隔离粒度仍待确认。

首版函数入口默认为 `fetch(payload)`，必须返回字典；批量结果通过稳定的 `asset_id` 匹配。Runner 按依赖集合缓存 uv 虚拟环境，单次执行默认 45 秒、最大 60 秒，并与业务数据库隔离。

## 10. 通知与外部推送

### 10.1 站内通知

所有事件先进入站内通知与事件 Log，包括：

- 定投计划到期。
- 数据源执行失败或恢复。
- 资产数据长时间未更新。
- 篮子比例偏离目标。
- 里程碑达到指定进度或正式解锁。
- 通用数值条件达到设定值。

### 10.2 外部 Webhook

外部推送使用通用 Webhook，不为具体通知服务设计专用适配器。

每个 Webhook 可配置：

- 名称和启用状态。
- POST URL。
- 自定义 Headers。
- 请求体模板。
- 有限的模板占位符。
- 测试发送。
- 失败重试。
- 执行日志。

Header 中的 Token 等敏感值只在通过平台鉴权后的设置页中用于编辑，执行日志不输出明文。

示例模板：

```json
{
  "title": "{{event.title}}",
  "message": "{{event.message}}",
  "asset": "{{asset.name}}",
  "currentValue": "{{asset.currentValue}}",
  "targetValue": "{{rule.targetValue}}",
  "triggeredAt": "{{event.triggeredAt}}"
}
```

### 10.3 通用触发规则

触发条件可绑定资产字段、篮子指标、总资产指标或里程碑进度，支持：

- `>`
- `>=`
- `<`
- `<=`
- `=`

每条推送规则增加频率参数：

```text
在指定时间窗口内，最多推送指定次数
```

例如：

```text
24 小时内最多推送 2 次
7 天内最多推送 1 次
```

里程碑可以在指定完成比例或正式解锁时触发推送。

条件在新的估值快照事件中重新检查；条件持续成立时，每次新事件均可触发，但受“时间窗口内最多 N 次”限制。里程碑正式解锁使用永久去重，只推送一次。

## 11. 当前安全与部署边界

当前暂定：

- Nginx Basic Auth 作为外层鉴权。
- 平台自身 Token 作为第二层鉴权；登录页校验成功后换取有限时的 `HttpOnly` 会话。
- Token 不嵌入前端构建产物，不写入 `localStorage` 或 `sessionStorage`。
- 登录失败进行有界的服务器端限速，退出登录会撤销服务器端会话并立即删除 Cookie。
- 凭证采用服务器密码级别的强度。
- 当前威胁模型不考虑凭证泄漏。
- Python 脚本在 Docker 中运行。
- 平台需要数据库持久化数据。
- 项目采用前后端分离架构。
- 后端使用 Python 3。
- 前端采用 React 19、TypeScript 与 Vinext；后端采用 FastAPI、SQLAlchemy 与 Alembic；数据库采用 MySQL 8.4。
- 是否直接暴露公网以及最终部署方式以后综合决定。

### 11.1 数据保留与备份

- 业务数据持久化到数据库。
- 业务记录不得物理删除；所有删除操作使用软删除。
- 被软删除的数据不再参与默认业务计算，但仍可审计和恢复。
- 资产估值按配置频率持续写入历史记录，而不是只覆盖当前值。
- 常规数据保护依赖数据库备份机制。
- 开发期间如需修改数据库表结构，执行迁移前必须先备份数据库。
- 迁移失败时应可以使用迁移前备份恢复。
- 备份保留周期、自动化方式和恢复演练频率仍待部署阶段确定。

## 12. 当前待确认事项

1. 动态分配建议是否允许用户在确认实际到账时手动覆盖。
2. Runner 的 CPU、内存、网络访问白名单与生产级隔离策略。
3. 数据库备份保留周期、自动化方式和恢复演练频率。
4. 下一阶段是否增加交易平台同步、指数/个股/基金查看能力。
